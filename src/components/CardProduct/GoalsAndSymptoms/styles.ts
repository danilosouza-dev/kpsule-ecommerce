import styled from "styled-components";

export const GoalsAndSymptomsContainer = styled.div`
    background: ${(props) => props.theme["gray-100"] };

    display: flex;
    flex-direction: column;
    gap: 0.75rem;

    padding: 0.625rem 1rem;
    margin: 1.5rem 0;

    border-radius: 8px ;
`
